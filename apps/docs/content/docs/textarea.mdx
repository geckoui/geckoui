---
title: Textarea
description: Multi-line text input with optional auto-resizing functionality
---

import { Textarea } from '@geckoui/geckoui';

# Textarea

A textarea that can grow to fit what is typed. `rows` sets where it starts, `maxRows` where it stops.

## Installation

```tsx
import { Textarea } from '@geckoui/geckoui';
```

## Basic Usage

<Preview>
  <Textarea placeholder="Type something..." />
</Preview>

```tsx
<Textarea placeholder="Type something..." />
```

## Props API

| Prop | Type | Default | Description |
|------|------|---------|-------------|
| `autoResize` | `boolean` | `false` | Enable automatic height adjustment based on content |
| `rows` | `number` | `2` | Minimum number of rows to display |
| `maxRows` | `number` | - | Maximum number of rows (only works with `autoResize`) |
| `className` | `string` | - | CSS class for styling |
| `disabled` | `boolean` | `false` | Disable the textarea |
| `readOnly` | `boolean` | `false` | Make the textarea read-only |
| ...rest | `TextareaHTMLAttributes` | - | All standard HTML textarea attributes |

## Examples

### Auto-Resizing Textarea

<Preview>
  <Textarea
    autoResize
    placeholder="This textarea grows as you type..."
  />
</Preview>

```tsx
<Textarea
  autoResize
  placeholder="This textarea grows as you type..."
/>
```

### With Row Constraints

<Preview>
  <Textarea
    rows={4}
    placeholder="Fixed at 4 rows"
  />
</Preview>

```tsx
<Textarea
  rows={4}
  placeholder="Fixed at 4 rows"
/>
```

### Auto-Resize with Max Rows

<Preview>
  <Textarea
    autoResize
    rows={2}
    maxRows={8}
    placeholder="Grows from 2 to 8 rows, then scrolls..."
  />
</Preview>

```tsx
<Textarea
  autoResize
  rows={2}
  maxRows={8}
  placeholder="Grows from 2 to 8 rows, then scrolls..."
/>
```

### Disabled State

<Preview>
  <Textarea
    disabled
    placeholder="This textarea is disabled"
    defaultValue="Cannot edit this content"
  />
</Preview>

```tsx
<Textarea
  disabled
  placeholder="This textarea is disabled"
  value="Cannot edit this content"
/>
```

### Read-Only State

<Preview>
  <Textarea
    readOnly
    defaultValue="This is read-only content that cannot be edited."
    rows={3}
  />
</Preview>

```tsx
<Textarea
  readOnly
  value="This is read-only content that cannot be edited."
  rows={3}
/>
```

## Auto-Resize Behavior

When `autoResize` is enabled:
- The textarea grows automatically as content increases
- Minimum height is determined by the `rows` prop
- Maximum height is determined by the `maxRows` prop
- Shrinks back down when content is removed

When `autoResize` is disabled it is a plain `<textarea>`: `rows` sets the height, nothing is
written to its `style`, so CSS can size it and the user can drag it. `maxRows` is ignored.

```css
.GeckoUITextarea {
  height: 200px;
}
```

## Accessibility

- Fully keyboard navigable
- Works with screen readers
- Supports all ARIA textarea attributes
- Disabled state is properly communicated

## Styling

The component uses the class name `GeckoUITextarea`. Override styles using the `className` prop:

```css
.GeckoUITextarea {
  /* Your custom styles */
}
```

## Related Components

- [Input](/docs/input) - Single-line text input
- [Label](/docs/label) - Form labels
- [InputError](/docs/input-error) - Error messages
- [RHFTextarea](/docs/rhf-textarea) - React Hook Form integration
