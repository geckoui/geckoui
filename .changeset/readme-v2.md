---
"@geckoui/geckoui": patch
---

Bring the package README up to v2. It showed `Button variant="contained"`, which is not a
variant, and `Alert variant="success"`, which is `color` now, and Quick Start never mounted
`GeckoUIProvider`.
